spring:
  application:
    name: twitterd

logging:
  level:
    io.lw900925: debug

app:
  base-url: https://api.twitter.com/1.1
  proxy:
    host: 127.0.0.1
    port: 7890
  access-token:
  screen-name:
  count: 200
  media-download-path: ${user.dir}
